# Archived Plans

This directory holds plan and design docs for shipped initiatives. They are
kept for searchability — `git log --follow` works on any moved file, and the
text often contains useful context for understanding why current code looks
the way it does.

Plans in the parent `docs/plans/` directory either describe active work or
are kept "Reference" status because their reasoning is still load-bearing
for current code.
